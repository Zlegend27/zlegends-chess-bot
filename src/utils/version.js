/* Bump this when evaluate(), search(), or the opening book change meaningfully
   so game history can be correlated against the engine build that produced it. */
export const ENGINE_VERSION = "1.1.0";
